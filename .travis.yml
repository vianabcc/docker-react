sudo: required
language: generic
services:
  - docker

before_install:
  - docker build -t vianabcc/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true vianabcc/docker-react npm run test
